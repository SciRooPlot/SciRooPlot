#!/bin/bash

export SCIROOPLOT_ROOT="@SCIROOPLOT_ROOT@"
export SCIROOPLOT_BIN="${SCIROOPLOT_ROOT}/@CMAKE_INSTALL_BINDIR@"
export SCIROOPLOT_LIB="${SCIROOPLOT_ROOT}/@CMAKE_INSTALL_LIBDIR@"
export SCIROOPLOT_DATA_DIR="${SCIROOPLOT_ROOT}/share/scirooplot"
export SCIROOPLOT_ENV="${SCIROOPLOT_DATA_DIR}/scirooplot-env.sh"
export SCIROOPLOT_SOURCE_DIR="@CMAKE_CURRENT_BINARY_DIR@/../"
export SCIROOPLOT_PYTHON_EXECUTABLE="@Python3_EXECUTABLE@"
if [ -n "${SCIROOPLOT_PYTHON_EXECUTABLE:-}" ]; then
    export PYTHONPATH="${PYTHONPATH:+$PYTHONPATH:}${SCIROOPLOT_LIB}"
fi

# enable completion only for interactive shells
case "$-" in
  *i*) ;;
  *) return 1 ;;
esac

if [[ ! -f "${SCIROOPLOT_BIN}/plot" || ! -f "${SCIROOPLOT_BIN}/srp" ]]; then
  echo "SciRooPlot is not installed. Cannot initialize plotting functions."
  return 1
fi

function plot() {
  local user_exec=$("${SCIROOPLOT_BIN}/srp" "get" "@current" "EXE")
  local isPython=0
  [[ "$user_exec" == *.py ]] && isPython=1
  local user_dir=$([[ -n "${user_exec}" ]] && dirname -- "${user_exec}" || echo "")
  if [[ -d "${user_dir}" ]]; then

    if [[ "${1}" == "cd" && -z "${2}" ]]; then
      cd "${user_dir}"
      return 0
    fi

    if [[ "${1}" == "open" ]]; then
      if command -v open >/dev/null 2>&1; then
        opener=(open)
      elif command -v xdg-open >/dev/null 2>&1; then
        opener=(xdg-open)
      elif command -v gio >/dev/null 2>&1; then
        opener=(gio open)
      elif command -v wslview >/dev/null 2>&1; then
        opener=(wslview)
      fi
      if [[ -n "${opener:-}" ]]; then
        if command -v find >/dev/null 2>&1; then
          if (( isPython )); then
            find "${user_dir}/" -maxdepth 2 -type f \( -name '*.py' \)  -exec "${opener[@]}" {} +
          else
            find "${user_dir}/.." -maxdepth 2 -type f \( -name '*.cxx' -o -name '*.cpp' -o -name '*.h' -o -name '*.hpp' \)  -exec "${opener[@]}" {} +
          fi
          return 0
        fi
      fi
    fi

    local user_plotdef="$(${SCIROOPLOT_BIN}/srp path "@current")/plots.info"
    (
      cd "${user_dir}";
      if (( ! isPython )); then
        (make -j6 1> /dev/null) || exit 1
      fi
      if [[ ! -f "${user_plotdef}" || "${user_exec}" -nt "${user_plotdef}" ]]; then
        if (( isPython )); then
          if [ -n "${SCIROOPLOT_PYTHON_EXECUTABLE:-}" ]; then
            if [ ! -x "${SCIROOPLOT_PYTHON_EXECUTABLE}" ]; then
              echo "SciRooPlot's Python path is outdated and requires reinstall..."
              "${SCIROOPLOT_SOURCE_DIR}/scripts/install.sh" --reinstall
            fi
            ${SCIROOPLOT_PYTHON_EXECUTABLE} "${user_exec}"
          else
              echo "SciRooPlot was not compiled with Python bindings."
          fi
        else
          "${user_exec}"
        fi
      fi
    ) || return 1
  fi
  "${SCIROOPLOT_BIN}/plot" "$@"
  return 0
}

function srp() {
  if [[ "${1}" == "update" ]]; then
    "${SCIROOPLOT_SOURCE_DIR}/scripts/install.sh" "${2}"
    source "${SCIROOPLOT_ENV}"
  elif [[ "${1}" == "cd" && -z "${2}" ]]; then
    cd "${SCIROOPLOT_SOURCE_DIR}/build"
  elif [[ "${1}" == "init-cpp" || "${1}" == "init-py" ]]; then
    local isPython=0; [[ "${1}" == "init-py" ]] && isPython=1
    local project_name="${2}"
    local project_path="${project_name}"
    [ -n "${3}" ] && project_path="${3}"
    if [[ "${project_path}" != /* ]]; then
      project_path="$(pwd)/${project_path}"
    fi
    if [[ -z "${project_name}" ]]; then
      echo "No project name specified. Doing nothing..."
      return 1
    fi
    if [[ "${project_name}" == "@current" ]]; then
      echo "A project cannot be called @current."
      return 1
    fi
    if [ -d "${project_path}" ]; then
      echo "Folder ${project_path} already exists. Doing nothing..."
      return 1
    fi

    mkdir -p "${project_path}"
    if (( isPython )); then
      cp "${SCIROOPLOT_DATA_DIR}/user/DefinePlots.py" "${project_path}/"
      sed -i.bak "s/PROJECT_NAME/${project_name}/" "${project_path}/DefinePlots.py" && rm -f "${project_path}/DefinePlots.py.bak"
      "${SCIROOPLOT_BIN}/srp" "set" "${project_name}" "EXE" "${project_path}/DefinePlots.py"
    else
      cp "${SCIROOPLOT_DATA_DIR}/user/CMakeLists.txt" "${project_path}/"
      cp "${SCIROOPLOT_DATA_DIR}/user/DefinePlots.cxx" "${project_path}/"
      sed -i.bak "s/PROJECT_NAME/${project_name}/" "${project_path}/CMakeLists.txt" && rm -f "${project_path}/CMakeLists.txt.bak"
      sed -i.bak "s/PROJECT_NAME/${project_name}/" "${project_path}/DefinePlots.cxx" && rm -f "${project_path}/DefinePlots.cxx.bak"
      mkdir -p "${project_path}/build"
      local curdir="$(pwd)"
      cd "${project_path}/build" && cmake -Wno-dev .. > /dev/null 2>&1 && make > /dev/null 2>&1 && ./definePlots
      cd "${curdir}"
      "${SCIROOPLOT_BIN}/srp" "set" "${project_name}" "EXE" "${project_path}/build/definePlots"
    fi
    "${SCIROOPLOT_BIN}/srp" "set" "${project_name}" "OUT" "${project_path}/output"
    "${SCIROOPLOT_BIN}/srp" "select" "${project_name}"
    "${SCIROOPLOT_BIN}/srp" "show" "${project_name}"
  else
    args=("$@")
    [[ -n "${ZSH_VERSION}" ]] && setopt local_options KSH_ARRAYS
    if (( ${#args[@]} >= 4 )) && [[ "${args[3]}" != /* ]]; then
      args[3]="$(pwd)/${args[3]}"
    fi
    "${SCIROOPLOT_BIN}/srp" "${args[@]}"
  fi
  return 0
}

_plot_completions_zsh() {

  local modes=('show' 'pdf' 'eps' 'svg' 'png' 'gif' 'file' 'inputs' 'find' 'macro' 'print')
  local groups
  local groups_and_categories
  local names
  
  local tabcomp_file="$(${SCIROOPLOT_BIN}/srp path "@current")/tabcomp.csv"
  if [[ ! -f "${tabcomp_file}" ]]; then
    return
  fi

  # first find groups and groups including categories
  local plotName figureGroup figureCategory
  while IFS="," read -r plotName figureGroup figureCategory
  do
    groups+=("${figureGroup}")
    [ -n "${figureCategory}" ] && groups_and_categories+=("${figureGroup}/${figureCategory}")
  done < <(LC_ALL=C sort -t',' -k2,3 -u "${tabcomp_file}")

  updatePlotNames() {
    local group category
    IFS="/" read -r group category <<< "${1}"
    names=()
    
    local plotName figureGroup figureCategory
    while IFS="," read -r plotName figureGroup figureCategory
    do
      [[ -n "${category}" && "${figureCategory}" != "${category}" && "${figureCategory}" != "${category}/"* ]] && continue
      names+=("${plotName}")
    done < <( grep "^.*,${group},${category}" "${tabcomp_file}" )
  }

  _arguments \
    '1: :->group'\
    '2: :->name'\
    '3: :->mode'


  case $state in
    group)
      if [[ "${words[2]}" == *"/"* ]]; then
        _arguments '1:profiles:(${groups_and_categories})'
      else
        _arguments '1:profiles:(${groups})'
      fi
    ;;
    name)
      if [[ "${words[2]}" == "cd" || "${words[2]}" == "open" ]]; then
        _arguments '2:profiles:()'
      else
        updatePlotNames "${words[2]}"
        _arguments '2:profiles:(${names})'
      fi
    ;;
    mode)
      _arguments '3:profiles:(${modes})'
    ;;
  esac

  unset -f updatePlotNames
}

_srp_completions_zsh() {

  local general_commands=('update' 'cd' 'init-cpp' 'init-py' 'projects' 'reset' 'clean' 'help')
  local project_commands=('remove' 'select' 'show' 'path' 'set' 'get')
  local project_commands_with_prop=('set' 'get')
  local properties=('EXE' 'OUT' 'NAME')
  local file_commands=('open' 'print')

  local projects
  local project_file="$(${SCIROOPLOT_BIN}/srp path)/projects.info"
  if [[ -f "${project_file}" ]]; then
    projects=("@current")
    local project
    while read -r project
    do
      projects+=("${project}")
    done < <(grep -B1 '{' "${project_file}" | grep -v '{' | grep -v -- '^--$')
  fi

  _arguments \
    '1: :->command'\
    '2: :->project'\
    '3: :->property'\
    '4: :->setting'

  case "${state}" in
    command)
      _arguments '1:profiles:(${general_commands} ${project_commands} ${file_commands})'
    ;;
    project)
      if [[ " ${project_commands[@]} " =~ " ${words[2]} " ]]; then
        _arguments '2:profiles:(${projects})'
      elif [[ " ${file_commands[@]} " =~ " ${words[2]} " ]]; then
        _files -g '*.root'
      fi
    ;;
    property)
      if [[ " ${project_commands_with_prop[@]} " =~ " ${words[2]} " ]]; then
        _arguments '3:profiles:(${properties})'
      elif [[ "${words[2]}" == init-* ]]; then
        _files -/
      fi
    ;;
    setting)
      if [[ "${words[2]}" == "set" ]]; then
        if [[ "${words[4]}" == "EXE" ]]; then
          _files -f
        elif [[ "${words[4]}" == "OUT" ]]; then
          _files -/
        fi
      fi
    ;;
  esac
}

_plot_completions_bash() {

  local modes=('show pdf eps svg png gif file inputs find macro print')
  local groups
  local groups_and_categories
  local names

  local tabcomp_file="$(${SCIROOPLOT_BIN}/srp path "@current")/tabcomp.csv"
  if [[ ! -f "${tabcomp_file}" ]]; then
    return
  fi

  # first find groups and groups including categories
  local plotName figureGroup figureCategory
  while IFS="," read -r plotName figureGroup figureCategory
  do
    groups+=" ${figureGroup}"
    [ -n "${figureCategory}" ] && groups_and_categories+=" ${figureGroup}/${figureCategory}"
  done < <(LC_ALL=C sort -t',' -k2,3 -u "${tabcomp_file}")

  updatePlotNames() {
    local group category
    IFS="/" read -r group category <<< "${1}"
    names=()
    
    local plotName figureGroup figureCategory
    while IFS="," read -r plotName figureGroup figureCategory
    do
      [[ -n "${category}" && "${figureCategory}" != "${category}" && "${figureCategory}" != "${category}/"* ]] && continue
      names+=" ${plotName}"
    done < <( grep "^.*,${group},${category}" "${tabcomp_file}" )
  }

  case "${COMP_CWORD}" in
  1)
    if [[ "${COMP_WORDS[COMP_CWORD]}" == *"/"* ]]; then
      COMPREPLY=( $(compgen -W "${groups_and_categories}" -- "${COMP_WORDS[COMP_CWORD]}") )
    else
      COMPREPLY=( $(compgen -W "${groups}" -- "${COMP_WORDS[COMP_CWORD]}") )
    fi
  ;;
  2)
    if [[ "${COMP_WORDS[COMP_CWORD-1]}" == "cd" || "${COMP_WORDS[COMP_CWORD-1]}" == "open" ]]; then
      COMPREPLY=( )
    else
      updatePlotNames "${COMP_WORDS[COMP_CWORD-1]}"
      COMPREPLY=( $(compgen -W "${names}" -- "${COMP_WORDS[COMP_CWORD]}") )
    fi
  ;;
  3)
    COMPREPLY=( $( compgen -W "${modes}" -- "${COMP_WORDS[COMP_CWORD]}" ) )
  ;;
  esac

  unset -f updatePlotNames
}

_srp_completions_bash() {

  local general_commands=('update cd init-cpp init-py projects reset clean help')
  local project_commands=('remove select show path set get')
  local project_commands_with_prop=('set get')
  local properties=('EXE OUT NAME')
  local file_commands=('open print')

  local projects
  local project_file="$(${SCIROOPLOT_BIN}/srp path)/projects.info"
  if [[ -f "${project_file}" ]]; then
    projects=("@current")
    local project
    while read -r project
    do
      projects+=("${project}")
    done < <(grep -B1 '{' "${project_file}" | grep -v '{' | grep -v -- '^--$')
  fi

  case "${COMP_CWORD}" in
  1)
    COMPREPLY=( $(compgen -W "${general_commands} ${project_commands} ${file_commands}" -- "${COMP_WORDS[COMP_CWORD]}") )
  ;;
  2)
    if [[ " ${project_commands[@]} " =~ " ${COMP_WORDS[COMP_CWORD-1]} " ]]; then
      COMPREPLY=( $(compgen -W "${projects[*]}" -- "${COMP_WORDS[COMP_CWORD]}") )
    elif [[ " ${file_commands[@]} " =~ " ${COMP_WORDS[COMP_CWORD-1]} " ]]; then
      cur="${COMP_WORDS[COMP_CWORD]}"
      while IFS= read -r d; do
        COMPREPLY+=("${d}/")
      done < <(compgen -d -- "${cur}")
      while IFS= read -r f; do
        [[ "${f}" == *.root ]] && COMPREPLY+=("${f}")
      done < <(compgen -f -- "${cur}")
    else
      COMPREPLY=( )
    fi
  ;;
  3)
    if [[ " ${project_commands_with_prop[@]} " =~ " ${COMP_WORDS[COMP_CWORD-2]} " ]]; then
      COMPREPLY=( $(compgen -W "${properties}" -- "${COMP_WORDS[COMP_CWORD]}") )
    elif [[ "${COMP_WORDS[COMP_CWORD-2]}" == init-* ]]; then
      COMPREPLY=( $(compgen -d -- "${COMP_WORDS[COMP_CWORD]}") )
    else
      COMPREPLY=( )
    fi
  ;;
  4)
    if [[ "${COMP_WORDS[COMP_CWORD-3]}" == "set" ]]; then
      case "${COMP_WORDS[COMP_CWORD-1]}" in
        EXE)
          COMPREPLY=( $(compgen -f -- "${COMP_WORDS[COMP_CWORD]}") )
        ;;
        OUT)
          COMPREPLY=( $(compgen -d -- "${COMP_WORDS[COMP_CWORD]}") )
        ;;
      esac
    fi
  ;;
  esac
}

if [ -n "${ZSH_VERSION}" ]; then
  if ! command -v compinit &> /dev/null; then
    autoload -U compinit compdef && compinit
  fi
  if command -v compdef &> /dev/null; then
    compdef _plot_completions_zsh plot
    compdef _srp_completions_zsh srp
  fi
elif [ -n "${BASH_VERSION}" ]; then
  if ! shopt -q progcomp; then
    BASH_COMPLETION_PATHS=(
      "/usr/share/bash-completion/bash_completion"
      "/etc/bash_completion"
      "/usr/local/etc/bash_completion"
      "/opt/homebrew/etc/bash_completion"
    )

    for bc in "${BASH_COMPLETION_PATHS[@]}"; do
      if [ -r "${bc}" ]; then
        source "${bc}"
        shopt -q progcomp && break
      fi
    done
  fi
  if shopt -q progcomp; then
    complete -F _plot_completions_bash plot
    complete -F _srp_completions_bash srp
  fi
fi
